# MadeInMagius Site

Personal terminal for MadeInMagius tools, archives and future projects.

- Static build: GitHub Pages compatible.
- Dynamic release bridge: prepared for Cloudflare Pages Functions.
- Source tool repositories remain the release authority.

Run `npm run check` and `npm run build` locally. Cloudflare deployment instructions live in `docs/CLOUDFLARE.md`.
